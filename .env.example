# bili_summary 配置示例
# 用法：复制本文件为 .env 使用，或直接运行 python bili_summary.py --setup 自动生成
# 优先级：真实环境变量 > 项目目录 .env 文件 > 代码默认值

# ── DeepSeek（仅“转写+AI总结”模式需要；只转写可留空）──
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MODEL_FALLBACK=deepseek-reasoner

# ── Whisper 模型规格 ──
# 内置可选: tiny / base / small / medium / large-v3（越大越准、越慢、越吃内存）
# 也可填本地模型目录绝对路径（跳过下载直接加载）
WHISPER_SIZE=small

# 推理设备：CPU 用户保持 cpu+int8；
# NVIDIA 显卡用户可改为 cuda + float16 大幅提速（需已装 CUDA 驱动）
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# ── 模型下载镜像与缓存位置 ──
# 中国大陆网络建议用镜像；缓存目录默认 ~/.cache/huggingface，可自定义（如 D:/models）
# HF_ENDPOINT=https://hf-mirror.com
# HF_HOME=~/models/whisper

# ── 其他 ──
# BILI_OUTPUT_DIR=./output
# BILI_VENV_DIR 必须是真实环境变量（.env 里设置不生效），默认 脚本目录/.venv
